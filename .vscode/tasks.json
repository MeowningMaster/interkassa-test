{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Watch",
            "type": "deno",
            "command": "run",
            "args": ["-A", "--watch", "--import-map=import_map.json", "--no-check", "src/main.ts"],
            "problemMatcher": ["$deno"]
        }
    ]
}
